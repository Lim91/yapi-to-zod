# yapi-to-zod README

vscode插件，通过yapi接口返回的数据生成`zod`接口定义文件

## Features

使用

使用快捷键`cmd+shift+p`调出命令窗口，输入`YapiToZod`，弹出两条命令。
* YapiToZod: 生成配置文件
* YapiToZod: 生成Api声明文件
> 注意：先生成配置文件，才可以生成Api声明文件


## Release Notes

### 0.0.1

* 支持yapi转zod接口定义文件

---
